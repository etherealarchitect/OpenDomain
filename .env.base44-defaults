# Base44 development defaults — placeholders so the app boots without credentials.
# Real secrets delivered to /run/base44/app.env always override these (last env_file wins).
ENVIRONMENT=development
EPP_SIMULATE=true
DNS_PROVIDER=powerdns
SECRET_KEY=base44-dev-secret-key-please-replace-in-production-32chars
